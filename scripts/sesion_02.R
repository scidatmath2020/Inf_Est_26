mi_primer_vector_numerico = c(1,-4,2026)
mi_primer_vector_logico = c(TRUE,TRUE,FALSE,TRUE)

no_vector = c(5,"hola")

mi_primer_lista = list(5,TRUE,"hola a todos")

lista_con_lista = list(FALSE,
                       list("A",5),
                       c(5,8,10)
                       )
2+6
# HOola a todos. Esta es una muy buena sesión de nuestro
# diplomado
5*8

###### R distingue entre mayúsculas y minúsculas
###### Es decir, no es lo mismo el texto "hola" que "Hola"

### Ejemplo

Vector1 = c(2,7)
vector1 = c(5,8)

vector_singulete <- c(TRUE)

c("hola a todos") -> vector_contrario

#### matrices

mi_primer_matriz = list(c(1,2,4),
                        c(-3,5,8),
                        c(-2,1,1)
                        )

otra_matriz = list(c("hola","adios"),
                   c("a","b"),
                   c("10","11")
)
                   
### Dataframes

mi_primer_dataframe = list(c("Luis","Pablo","Jimena"),
                           c(16,18,18)
                           )
 
 
  